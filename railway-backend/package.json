{
  "name": "devpersonality-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "start": "npx prisma generate && node server.js",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@prisma/client": "^5.22.0",
    "prisma": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 